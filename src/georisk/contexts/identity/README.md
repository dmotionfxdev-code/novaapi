# Identity & Tenancy

**Populated in:** Roadmap Sprint 1 (basic Tenant/User/JWT), hardened in Sprint 11 (RLS, schema-per-tenant, entitlements).
**Design reference:** Domain Model §1/§3 (`Tenant`, `User` aggregates), Platform Architecture §4 (tenant boundaries), Infrastructure Architecture §5/§6 (tenancy + RLS).

Empty by design — Sprint 0 provides only the folder and the import-boundary contract that will govern it.
