# Notification

**Populated in:** Roadmap Sprint 10.
**Design reference:** Domain Model §1 (`NotificationRule`, `AlertInstance`, `NotificationDispatch`), Domain Model §7 (pure event-driven, decoupled integration — no other context calls into this one directly).

Empty by design — Sprint 0 provides only the folder and the import-boundary contract that will govern it.
