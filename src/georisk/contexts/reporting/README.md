# Reporting

**Populated in:** Roadmap Sprint 8.
**Design reference:** Domain Model §1 (`Report` — immutable snapshot invariant), Application Layer §6 (`ReportSnapshotBuilder`).

Empty by design — Sprint 0 provides only the folder and the import-boundary contract that will govern it.
